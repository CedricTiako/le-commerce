RewriteEngine On

# Ne pas réécrire les fichiers/dossiers existants (assets, images, css, js...)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Tout le reste passe par le front controller
RewriteRule ^ index.php [L]
